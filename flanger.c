#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <math.h>

// #include "main.h"

#define f = //fréquence de l'effet en Hz
#define g = 1 //intensité de l'effet entre 0 et 1
#define M0 = 1 //moyenne du delay 
#define A = //balayage

float flanger (fp)
{
	int s =[len(fp)]
	for n in range
	{
	M[n] = M0*(1+f*2*n*T*3.14):
	y = x[n] + gx[n-M[n]];
	
	s = s+y;
	}
	return s
}
